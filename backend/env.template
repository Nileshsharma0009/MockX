# ============================================
# Backend Environment Variables Template
# ============================================
# Copy this file to .env and fill in your actual values
# Command: cp env.template .env
# DO NOT commit .env to git (it's in .gitignore)

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development
# For production, set: NODE_ENV=production

# ============================================
# Database
# ============================================
# MongoDB connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URL=your_mongodb_connection_string_here

# ============================================
# JWT Authentication
# ============================================
# Generate a strong random secret key (at least 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins (no spaces after commas)
# For local development: http://localhost:5173,http://localhost:5174
# For production: https://mock-x.vercel.app,https://www.mock-x.vercel.app
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,https://mock-x.vercel.app

# ============================================
# Email Configuration (for OTP)
# ============================================
# Gmail account for sending OTP emails
EMAIL_USER=your_email@gmail.com

# Gmail App Password (NOT your regular password)
# To generate: Google Account → Security → 2-Step Verification → App Passwords
# Create an app password for "Mail" and use that here
EMAIL_PSAS=your_gmail_app_password_16_characters

# ============================================
# Optional: Alternative Environment Variable
# ============================================
# Some parts of the code also check NODE_ENVIRONMENT
# Usually not needed if NODE_ENV is set correctly
# NODE_ENVIRONMENT=production

